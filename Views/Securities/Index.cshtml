@model IEnumerable<AllForOne_Store.Models.Security>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Securities</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Action)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ActionDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Action)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ActionDate)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SecurityId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SecurityId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SecurityId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
